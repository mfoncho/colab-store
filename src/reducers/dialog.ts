
export const state = {};

export const reducers = {
    SET_DIALOG(state: any, {
        payload
    }: any ){
        return state;
    },
    LOCK_DIALOG( state: any, {
        payload
    }: any ){
        return state;
    },
    UNLOCK_DIALOG( state: any, {
        payload
    }: any ){
        return state;
    }
};

export default { state, reducers }
